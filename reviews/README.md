# reviews/ — 审读与基线

| 文件 | 说明 |
|---|---|
| `review-2026-08-23.md` | 2026-08-23 逐章审读报告（历史事实，原样保留，不篡改其声称的范围）。 |
| `baseline-2026-08-24.md` | Task Group 0 仓库机器基线（脚本生成，幂等可重跑）。 |
| `baseline_audit.py` | 生成基线报告的审计脚本。 |

## 结构范围可机器计算

全书的结构覆盖范围（27 章 + 附录 A–F 是否齐全、章号是否连续、每章是否六段、目录是否一致）由 `tools/check_book.py` 依据 `book.yml` 自动核验，并在 CI 的 `book-structure` job 中运行——不再依赖人工声明的"审读范围"。

> 待办（Task Group 4 §9.5 剩余项）：一份覆盖 27 章 + 附录 A–F + README + outline + 图表规范 + `book.yml` + 示例工程 README 的**完整编辑性复核报告**（区别于本目录的结构性机器核验），属编辑性再通读，单列为后续里程碑。
