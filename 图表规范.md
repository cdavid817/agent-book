# 图表规范

本书三类图各用一种工具，按"图要回答什么"分工：

| 图的类型 | 工具 | 理由 | 存放 / 渲染 |
|---|---|---|---|
| **架构、分层** | **D2** | 声明式、自动布局，容器/分层表达力强，适合"由哪几块构成、谁在哪一层" | 源 `diagrams/d2/*.d2` → 渲染 `assets/*.svg`，正文用 `![](../assets/x.svg)` 引用 |
| **时序、流程、状态** | **Mermaid** | GitHub 原生内联渲染，`sequenceDiagram / flowchart / stateDiagram` 表达步骤与分支最直接 | 直接写在正文 ```mermaid 代码块 |
| **讲原理的示意图** | **Excalidraw** | 手绘感、自由排布，适合"直觉/机制"这类不规则示意（如注意力、U 型曲线的手绘版） | 源 `diagrams/excalidraw/*.excalidraw` → 导出 `assets/*.svg`，正文 `![]()` 引用 |
| 数据图（占比/曲线） | Mermaid `pie` / `xychart` | 轻量、内联 | 直接写在正文 |

## 关键约束（先读）

- **只有 Mermaid 在 GitHub 上内联渲染**。D2 与 Excalidraw 都产出 SVG 文件，必须提交到 `assets/` 并用 `![]()` 引用——GitHub 会把提交的 SVG 当图片显示。
- **D2 渲染中文必须指定中文字体**：d2 默认字体不含 CJK 字形，会渲染成方块。用 `--font-regular` 传一个**单体 TTF/OTF**中文字体（`.ttc` 字体集不被接受）。构建脚本 `diagrams/render.sh` 已自动探测系统中文字体，也可用 `D2_CJK_FONT=/path/to/cjk.ttf` 覆盖。
- **Excalidraw 是手绘工具**，图需人工在 excalidraw.com 或 VS Code 插件里画，导出 SVG 到 `assets/`。仓库里 `diagrams/excalidraw/*.brief.md` 是每张示意图的"画什么"说明书，供绘制者据以作图。

## 配色（三工具统一）

主色 `#4F6D7A`、强调 `#C0D6DF`、警示 `#DD6E42`、中性 `#E8DAB2`；文字在深色块上用白色。D2 用 `classes` 定义（见 `diagrams/d2/ch12-harness.d2` 的 `classes` 块），Mermaid 用 `classDef`，Excalidraw 手动取色。

## 构建

```bash
bash diagrams/render.sh          # 渲染全部 D2 → assets/*.svg
D2_CJK_FONT=/path/to/font.ttf bash diagrams/render.sh   # 指定中文字体
```

Excalidraw 导出需人工操作（无命令行批渲）。

## 现状与迁移（渐进，不回归）

全书现有约 92 张图。迁移原则是**不破坏 GitHub 现有渲染**：Mermaid 时序/流程图**保持内联**（它们本就该用 Mermaid）；架构/分层图**逐张**转 D2——先提交 SVG、确认 GitHub 显示正常，再把对应 ```mermaid 块换成 `![]()`。已完成的示范：**第 12 章图 1（Harness 六大件）**，源见 `diagrams/d2/ch12-harness.d2`。

按类型分布（自动归类）：约 41 张时序/流程/状态（Mermaid 保留）、2 张数据图（保留）、约 48 张 graph（其中真正的架构/分层逐步转 D2，概念关系/决策树类可留 Mermaid 或按需转 Excalidraw 示意）。待转 D2 的架构/分层旗舰图：

- 第 1 章图 4：贯穿项目最终架构预告
- 第 12 章图 1：Harness 六大件 ✅（已转）
- 第 14 章图 1：四层 Span 树
- 第 9 章图 1：三层沙箱防线
- 第 25 章图 1：四层十六模块终态架构
- 第 20 章图 1：三档部署光谱

> 每转一张：`diagrams/d2/` 加源 → `render.sh` 出 SVG → 确认 GitHub 渲染（尤其中文）→ 正文换 `![]()` → 提交。
