# 全书逐章审读报告（2026-08-23）

> 审读范围：25 章正文 + 附录 A/B + README，共 28 个文件。
> 方法：自动化结构检查（六段结构/图数量与图注/图编号/坑与面试题数量与格式/代码围栏/交叉引用合法性/Mermaid 语法风险）+ 内容级逐章审读 + 全部受影响代码重新提取回归测试。

## 一、已修复的问题（本次审读中处置完毕）

| # | 严重度 | 位置 | 问题 | 处置 |
|---|---|---|---|---|
| 1 | 高 | 第 9 章 `SandboxedShell` | **安全漏洞**：`find` 在白名单（readonly），但 `find -exec rm` / `-delete` 可借只读命令执行删除——恰是本章场景事故一的原始攻击形态，代码却没防住 | 新增 `FORBIDDEN_ARGS`（-exec/-execdir/-delete/-ok/-okdir），classify 阶段拒绝；7 组回归用例通过 |
| 2 | 高 | 第 21 章 `guard_sql` | **安全漏洞**：黑名单漏掉 `REPLACE INTO` / `MERGE` / `PRAGMA` / `VACUUM`——REPLACE 是写入，可绕过只读白名单 | 补入 FORBIDDEN 正则；5 组新用例 + 原回归全部通过 |
| 3 | 中 | 7 个章节 | 图编号与出现顺序不一致（编号按写作规格、摆放按叙事）：第 1/7/12/15/18/21/23 章 | 按出现顺序受控重编号；章内引用同步重映射；跨章引用（第 2 章引第 1 章图、附录 B 引第 12 章图等）逐一核对修正 |
| 4 | 高（自伤事故） | 上述 7 章 | 重编号脚本用 `##` 作占位符导致 Markdown 标题的 `##` 被误删（`## 1.`→` 1.`、`###`→`#`） | 按确定性损坏模式全量重建标题；结构终检 25 章通过；此事故本身即第 15 章"变更必须过回归"的现身说法 |
| 5 | 低 | 第 1 章图 2 图注 | 重编号后"（此处提前给出，便于对照）"说明失效 | 删除该括注 |
| 6 | 低 | 第 24 章（前次已修） | DPO 负例定义把"自报成功未验证"轨迹当负例 | 已改为显式失败集合 + `skipped_ambiguous`（留档备查） |

## 二、已补充的内容（小幅增补）

| # | 位置 | 补充 |
|---|---|---|
| 1 | 第 5 章 2.2 | 说明 60k 字符演示阈值与"窗口 60–70% 水位"的关系（教学取小 vs 生产按窗口百分比） |
| 2 | 第 12 章 2.5 | 事件类型集声明为可扩展，并前向指认后续章节的增补（compaction / node_* / message）及扩展纪律 |
| 3 | 第 18 章代码 | interrupt 的教学简化注释：下一跳在挂起前确定、人工输入不参与路由，及生产做法 |
| 4 | 第 14 章代码 | OtelBridge 单会话单槽的简化注释 |
| 5 | 第 25 章 README | 教学简化清单从 6 条扩为 7 条（补 interrupt 路由限制与 OTel 单槽） |

## 三、逐章审读结论

| 章 | 结论 | 遗留建议（可选，未处置） |
|---|---|---|
| 1 | 通过。演进/教训/五要素/边界四块齐，Workflow 一节衔接自然 | 无 |
| 2 | 通过。协议细节与当前 API 一致，坑均有真实错误形态 | 无 |
| 3 | 通过。状态机/预算/内外循环/流式四块与后文引用网一致 | 无 |
| 4 | 通过。谱系-三维表-口诀链条完整，论文数据标注清楚 | 无 |
| 5 | 通过（本次补阈值说明）。压缩边界 bug 在写作期已被测试抓出并内化为坑 1 | 无 |
| 6 | 通过。三控制面决策树与第 13 章 PEP 衔接干净 | 无 |
| 7 | 通过。重试器行为测试覆盖四路径 | 无 |
| 8 | 通过。假 Server 端到端验证过握手/桥接/指纹 | SSE 废弃时间线如需精确到协议版本号可再核 |
| 9 | 通过（本次修复 -exec 漏洞）。三层防线与分级完整 | 无 |
| 10 | 通过。两套术语映射按修订版要求完成 | 无 |
| 11 | 通过。标题入索引的修复已内化为正文最佳实践 | 无 |
| 12 | 通过（本次补事件扩展说明）。shadow git 测试含"用户 .git 不受污染"用例 | 无 |
| 13 | 通过。PDP/PEP 十组用例含污点链拦截 | 无 |
| 14 | 通过（本次补单槽注释）。真实 OTel SDK 验证过树形与指标 | 无 |
| 15 | 通过。runner 的 BLOCK 语义有测试 | 无 |
| 16 | 通过。台账手算核对；单价表注明"从配置加载" | 价格数字建议出版前终校一次 |
| 17 | 通过。八拓扑四元组齐整，无代码章的 Checklist 交付合规 | 无 |
| 18 | 通过（本次补 interrupt 注释）。有界回边/幂等恢复有测试 | 无 |
| 19 | 通过。判定法数字链完整，归因测试通过 | 无 |
| 20 | 通过。评分卡三设计意图明确 | 无 |
| 21 | 通过（本次修复 REPLACE 漏洞） | 无 |
| 22 | 通过。OpenSpec 变更包与第 3/12/15 章机制严丝合缝 | 无 |
| 23 | 通过。修复环端到端真实跑通（真 pytest、教训链生效） | 无 |
| 24 | 通过。成熟度标注齐全，DPO 修复留档 | 无 |
| 25 | 通过。双联时序共用一条图注（图注已注明"两联"，属有意） | 无 |
| 附A | 通过。A.1 按指定结构，各组末尾引用清单齐 | 无 |
| 附B | 通过。三段结构严格，导读含分诊示例 | 无 |

## 四、终检状态

- 六段结构 / 坑与面试题数量与三要素格式 / 结论先行格式：25 章全部通过
- 图：每章 ≥2 张、编号按出现顺序连续、每图有"回答什么问题"式图注；Mermaid 语法风险扫描无告警
- 交叉引用：章号 1–25、附录 A.1–A.7 / B.1–B.6 全部合法；跨章图引用五处全部核对
- README：29 个链接全部有效
- 代码：本次改动的第 9/14/18/21 章重新提取并回归通过；其余章沿用写作期测试结论

## 五、留给作者的三条全局建议（不影响交付）

1. **出版前数字终校**：模型单价（第 16 章）、协议版本号（第 8 章）、工具生态描述（第 15 章表格）属于时效性内容，付印前按当时事实过一遍。
2. **建立本书自己的"回归集"**：本次审读的自动化检查脚本值得固化为 `tools/check_book.py`，任何章节改动后一键复检——这正是全书评测思想对写作过程自身的应用。
3. **贯穿项目可考虑真实落库**：目前代码散在章节内（已全部测试），若把 `vanehub-assistant/` 作为独立仓库落地并接 CI，第 25 章的 tag 与 README 即可从"书中示例"变为"可克隆资产"。
