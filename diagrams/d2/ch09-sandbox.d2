# 第9章 图1：三层沙箱防线
direction: down
classes: {
  threat: { style: { fill: "#DD6E42"; font-color: "#ffffff"; stroke: "#DD6E42"; border-radius: 8 } }
  layer:  { style: { fill: "#C0D6DF"; stroke: "#4F6D7A"; border-radius: 8 } }
  asset:  { style: { fill: "#4F6D7A"; font-color: "#ffffff"; stroke: "#4F6D7A"; border-radius: 8 } }
}
CODE: "Agent 生成的命令/代码\n（不可信输入）" { class: threat }
L3: "第三层：文件系统白名单\n拦: 越权读(密钥/他人工作区)、越权写\n仅挂工作区 · realpath 校验" { class: layer }
L2: "第二层：权限降级\n拦: 提权链、系统篡改\n非root · capability 裁剪 · 只读 rootfs" { class: layer }
L1: "第一层：容器/microVM 隔离\n拦: 宿主逃逸、资源耗尽、网络外传\n默认断网 + 出口白名单" { class: layer }
HOST: "宿主与企业内网\n（受保护资产）" { class: asset }
CODE -> L3 -> L2 -> L1 -> HOST: 层层拦截
